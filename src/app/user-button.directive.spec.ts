import { UserButtonDirective } from './user-button.directive';

describe('UserButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new UserButtonDirective();
    expect(directive).toBeTruthy();
  });
});
